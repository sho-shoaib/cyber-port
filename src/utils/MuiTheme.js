import { createTheme } from "@mui/material";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#FFFFFF",
      light: "#F5F5F7",
    },
    secondary: {
      main: "#FF4532",
    },
  },
});
